# Package entry point

Read [the workspace guidance](../AGENTS.md) before working in this package. Its paths and commands are relative to this directory. If that guidance is already loaded, do not read it again.

This entry point supports launching Codex directly here: this export has no Git root, so parent instruction discovery cannot be assumed. It adds no separate gates. If the package is moved on its own, replace this pointer with the workspace guidance and adapt its workspace-root paths to the new layout.
